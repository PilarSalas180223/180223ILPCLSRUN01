# OBTENER UN NUMERO Y DETERMINAR LO SIGUIENTE:
# A) SI ES NEGATIVO Y MENOR A -100 IMPRIMIR LOS NUMEROS IMPARES DE FORMA DESCENDENTE.
# B) SI ES POSITIVO Y MENOR A 100 IMPRIMIR LOS NUMEROS PARES DE FORMA ASCENDENTE.
# C) EN OTRO CASO IMPRIMIR NO VALIDO.

# Crear las variables

numero = float(input("Ingresa un numero: "))

# Procesos (Cálculos Matemáticos y/o Lógicos)
if (numero < 0 and numero >- 100):
    print(f"Cumple")

    for i in range(-1,-101,-2):
        print(i,"Numero Impar")

elif (numero >0 and numero <100):
    print(f"Cumple dos")

    for i in range(0,100,2):
        print(i,"Numero Par")

elif (numero <=100 or numero == 0 or numero >= 100):
    print(f"Numero invalido")
